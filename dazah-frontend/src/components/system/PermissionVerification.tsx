"use client"

import { PermissionRolloutManager } from "./PermissionRolloutManager"

/** Read-only view of each module's permission integration gate. */
export function PermissionVerification() {
  return <PermissionRolloutManager />
}
