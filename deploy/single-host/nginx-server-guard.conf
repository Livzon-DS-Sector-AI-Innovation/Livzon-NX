# Include inside the site's server block. The public/ directory is a read-only mount.
if (-f /run/dazah/maintenance) { return 503; }
limit_req_status 429;
limit_conn_status 429;
add_header Retry-After $dazah_retry_after always;
