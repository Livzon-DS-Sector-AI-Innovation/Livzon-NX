# Hermes V2 未完成任务清单

> 更新日期：2026-08-03
> 当前状态：本地代码、契约、迁移和全量自动化门禁已收口，等待真实飞书、
> 单消费者切换、镜像门禁和生产闭环验收。
> 说明：本清单只记录尚未完成或尚未形成验收证据的事项；mock、单元测试和页面展示
> 不能替代真实飞书及生产验收。

## P0：真实飞书与安全边界

- [ ] 使用指定测试飞书 App 完成私聊、群聊 `@Livzon`、Thread/引用、Reaction、
  图片、文件、PDF、音视频和富文本增量更新验收。普通文本私聊已于
  2026-08-03 通过真实测试 App 验收，图片、纯文本文件和文本型 PDF 私聊也已通过；
  音频的入站、安全降级和缓存清理已通过，但语音转写尚未接入；群聊按本轮验收范围
  暂缓，视频因当前业务消息不会涉及该类型，经用户确认从本轮范围排除；其余文件格式
  与交互项仍待验证。
- [ ] 验证未绑定用户、暂停/撤销用户、非白名单群、群聊未 `@Livzon` 均失败关闭，
  并保留不含消息正文和凭证的 Trace 证据。
- [ ] 验证中高风险确认卡片：原请求人、他人点击、重复点击、过期点击、状态冲突和
  部分成功场景。
- [ ] 验证主动文本/卡片投递、相同幂等键、可重试失败、最终失败和真实飞书回执。
- [ ] 验证同一消息只产生一个 Run 和一个回复气泡，包括最终编辑连续失败场景。
- [ ] 验证生产只有一个飞书事件消费者、旧连接关闭、回滚开关、凭证真实轮换及
  Secret 不回显。
- [ ] 验证 Base `record-list/search/get`、两轮表名选择、Scope 与资源授权错误提示。
- [ ] 按运行手册完成生产冒烟和回滚演练，并归档脱敏证据。

完成条件：上述项目均有测试时间、测试 App/租户标识、trace_id、结果和验收人；
不得记录 Secret、Token、Cookie、消息正文或未脱敏外部响应。

## P0：V2 与平台体验

- [ ] 覆盖事件重复、乱序、断线恢复、90 秒超时和心跳的真实前后端集成测试。
- [ ] 在真实后端和飞书环境验证停止生成、附件、历史恢复、多个确认项、确认过期、
  部分成功及实际 90 秒超时；上述交互的 Playwright mock E2E 已通过。Web 停止生成
  及停止后历史恢复、多个确认项、部分成功、确认过期、实际 90 秒超时与后续请求
  恢复均已通过真实验收。
- [ ] 验证 Web 与飞书同一请求的权限、风险、确认、错误码和结果语义一致。

## P1：身份、目录与策略治理

- [ ] 补齐 open_id/user_id/union_id、多租户/多 App、冲突、伪造主体和跨租户访问
  集成测试。
- [ ] 验证新增业务模块工具无需修改 Hermes 即可发现，并覆盖目录版本、删除、
  版本冲突和权限防枚举。
- [ ] 工具禁用影响分析补齐 Skill、自动化和用户流程的完整反向引用；当前页面已提供
  确认入口，但影响数据取决于现有注册关系。
- [ ] 统一验证 Web、飞书和自动化 ALLOW/DENY/CONFIRM 矩阵、“风险只升不降”
  以及人工责任判断禁止代理。
- [ ] 将尚未接入 Agent Tool Registry 的业务模块按优先级逐模块接入。

## P1：授权、Trace 与运维

- [ ] 完成授权与确认、Trace 与投递页面的真实数据量压测和服务端分页验收。
- [ ] 增加高风险操作禁止记忆授权的后端集成测试和界面提示。
- [ ] Trace 在现有入站消息、工具调用、确认、领域事件和飞书投递关联基础上，继续
  补齐能力搜索与审计收据的持久关联；继续禁止展示业务正文和凭证。
- [ ] 将真实飞书验收证据状态接入运行总览；当前消费者、最近配置变更和异常 Trace
  跳转已接入。

## 发布门禁

- [ ] 在获授权的发布流程构建并扫描后端生产镜像；开发 Compose 镜像、全量 pytest、
  branch coverage 和 migration upgrade/downgrade 已通过。
- [ ] 在获授权的发布流程构建并验证 Hermes 只读生产镜像；开发镜像、固定上游安装、
  全量测试与 Ruff 已通过。
- [ ] 在获授权的发布流程构建并扫描前端生产镜像；开发镜像、lint、typecheck、unit、
  coverage 命令、关键 E2E 和 production build 已通过。
- [ ] `python scripts/check-test-impact.py --base <目标分支> --head HEAD`。
- [ ] 单消费者证明、真实飞书验收、生产冒烟和回滚演练。

## 下一工作单元

1. 用户指定测试飞书 App、租户、私聊/群聊会话和验收人。
2. 依据 `docs/runbooks/hermes-native-feishu-gateway-phase0-runbook.md` 完成 P0
   真实验收并记录脱敏 Trace。
3. 对形成提交的最终变更运行 test-impact/diff coverage 门禁。
4. 在获授权的发布流程完成生产镜像、灰度和回滚门禁。
5. 完成全部真实验收后再将 Hermes V2 标记为生产闭环。

## 2026-08-03 本地收口证据

- 真实飞书普通文本私聊已通过：身份解析、持久会话准备、LLM 调用和会话完成接口
  均返回 `200`，回复包含约定的首尾标记；最近会话只产生 1 条用户消息和 1 条助手
  消息，入站原子收据状态为 `completed`，Gateway 保持单消费者。脱敏关联标识：
  trace_id `29e8838f-546d-44fa-8fe3-5b94fcca3e20`，run_id
  `379229a5-215b-45ac-913b-96154e3c434c`。
- 同一私聊的第二轮历史恢复已通过：仍复用同一个平台会话，会话内累计 2 条用户消息
  和 2 条助手消息，并准确回忆第一轮指定内容；第二轮入站收据为 `completed`。
  脱敏关联标识：trace_id `2b244a57-93a0-4599-9e1a-cfeb95ca9333`，run_id
  `f7a96a15-599c-49f7-af18-27f7100a5096`。
- Hermes 开发容器重启后的第三轮恢复已通过：Gateway 恢复为单消费者后仍复用同一
  平台会话，会话内累计 3 条用户消息和 3 条助手消息，并准确回忆第一轮第三段；
  第三轮入站收据为 `completed`。脱敏关联标识：trace_id
  `43ff2d7f-4078-4c01-a74b-92a056ab26c1`，run_id
  `dd1a5fe8-b35d-4e1b-94d5-4f3c6acb24a7`。
- 真实飞书私聊图片已通过链路验收：同一会话累计 4 条用户消息和 4 条助手消息，
  最新用户消息持久化 1 个 `image` 附件元数据，模型返回非空图片说明，入站收据为
  `completed`，处理后的受控临时文件已清理。脱敏关联标识：trace_id
  `da3f5ab7-f4f6-4fdd-954d-d90247e505d7`，run_id
  `eca381cb-b88c-4601-8f77-d72cb63e63c1`。
- 首次真实 PDF 私聊验收未通过：飞书将文件和说明文字作为两个独立事件送达，文件
  事件正文为空，持久会话准备因 `message` 最小长度校验返回 `422`；独立文字事件
  无法关联 PDF，因此模型安全降级为无法读取。Gateway 已为仅附件事件生成默认的
  非敏感分析指令，附件/网关定向回归 `29 passed`，开发容器重启后恢复单消费者。
- 第二次真实 PDF 私聊证明仅附件事件已不再返回 `422`，但模型只能看到受控缓存路径，
  未获得 PDF 正文，并在回复中暴露了容器内部路径，因此仍判定未通过。Hermes 现已
  增加受限 PDF 文本提取（最多 50 页、20 万字符），并统一移除文档、音频和视频的
  内部路径；扫描件、加密件和不支持格式仅返回脱敏提示。附件/Gateway 定向回归
  `32 passed`，`hermes-lite:dev` 镜像重建成功，运行态健康且保持 1 个事件消费者。
- 第三次真实 PDF 私聊已通过：入站原子收据为 `completed`，平台会话持久化 1 个
  `document` 附件元数据并生成 1 条助手回复；脱敏断言确认回复不含内部缓存路径和
  “无法读取”提示，并识别出文档标题及主要内容。脱敏关联标识：trace_id
  `9c0db4e5-69b3-4e89-a9cf-1e3f0333dda0`，run_id
  `888b9350-9a82-4f98-9b9a-ef12abe81339`。
- 补充附件生命周期治理：Gateway 在本轮处理结束或重复消息被抑制后删除受控缓存内
  的对应附件，拒绝删除 Hermes 缓存根目录以外的路径；附件、Gateway、渠道上下文
  与运行态定向回归 `71 passed`，开发镜像再次重建后健康且保持 1 个事件消费者。
  前两次失败验收遗留的 3 个 PDF 缓存文件未擅自删除，等待按数据处置权限清理。
- 真实飞书纯文本文件私聊已通过：入站原子收据为 `completed`，平台会话持久化 1 个
  `document / text/plain` 附件元数据并生成 1 条助手回复；脱敏断言确认回复包含主要
  内容，不含内部缓存路径、“无法读取”或“附件不可读取”提示。处理后 5 分钟内的
  文本/其他新缓存文件数量均为 0，证明本轮附件清理已生效。脱敏关联标识：trace_id
  `9710a40a-23b7-4322-abdc-521a49f02078`，run_id
  `fad1e10c-0a26-48e4-a26f-9150a108e2b6`。
- 真实飞书音频私聊完成部分验收：入站原子收据为 `completed`，附件正确识别为
  `audio / audio/mpeg`，回复不含内部路径或运行异常，本轮临时音频已删除，Gateway
  仍为 1 个事件消费者；但回复只能说明无法转写并请用户提供文字，因此内容理解功能
  判定未通过。当前容器没有显式受治理的 STT 配置，禁止直接复用通用 LLM Key 将
  音频发送至未确认的外部转写端点。脱敏关联标识：trace_id
  `297ae8c1-1bb2-4a12-8dd1-40140c7b9855`，run_id
  `3186efae-e746-440b-a30c-b85f63667707`。
- 视频文件私聊不属于当前业务消息场景，用户于 2026-08-03 明确确认跳过；本项记录为
  本轮范围排除，不作为通过证据，也不阻塞当前私聊验收，但若未来开放视频消息需重新
  完成入站、内容理解、脱敏、大小限制和缓存清理验收。
- 真实飞书富文本增量测试的服务端证据已通过：入站原子收据为 `completed`，同一
  trace 仅持久化 1 条助手消息且只有 1 个 run_id；最终回复同时包含二级标题、加粗、
  编号 1 至 8 和总结，不含内部缓存路径，Gateway 保持 1 个事件消费者。用户已从
  飞书界面确认全过程始终只有 1 个回复气泡且最终编辑完成，本项真实验收通过。
  脱敏关联标识：trace_id
  `974ed57a-5520-434c-b194-b1e2f36015b6`，run_id
  `5f13595d-b50e-46ca-aac3-286709d3d3d7`。
- 真实飞书私聊引用回复已通过：入站原子收据为 `completed`，平台消息元数据记录了
  外部引用关系并继续复用原会话；同一 trace 只有 1 个 run_id 和 1 条助手消息，回复
  严格收敛为被引用内容的标题且不含内部路径。脱敏关联标识：trace_id
  `1e0e55fc-798e-43a2-a595-fcbd947050a3`，run_id
  `aea5bc6d-dad4-4370-8004-01759606e940`。
- 真实飞书私聊 Reaction 已通过：用户对助手消息添加 👍 后没有产生新回复；服务端
  最新入站收据指纹、最新平台消息时间、trace_id 和 run_id 均保持为引用回复上一轮，
  证明 Reaction 未被误识别为新对话或重复触发 Agent。Gateway 同时保持 1 个事件
  消费者。
- 真实飞书身份停用失败关闭已通过：管理员经“身份与准入”页面将当前绑定临时暂停后，
  用户私聊只收到“飞书身份尚未绑定或未获得助手准入”的拒绝提示；Gateway 入站收据
  正常完成，但暂停期间平台 `agent_messages` 新增数为 0，证明请求未进入 Agent Run。
  随后同一管理页面已将绑定恢复为 `active`，暂停与恢复接口均返回 `200` 并记录审计；
  无需重启 Gateway 即完成恢复消息，入站收据为 `completed`，同一 trace 只有 1 个
  run_id 和 1 条助手消息，准确回复“恢复成功”。恢复验证脱敏关联标识：trace_id
  `c12d2923-e2c5-4a57-8477-e28748956d38`，run_id
  `ea798642-d76a-41be-807b-305fa1552212`。
- 本次真实操作同时发现身份状态接口序列化 `updated_at` 时触发 `MissingGreenlet` 并返回
  `500`；已在状态写入后显式刷新 ORM 实体，身份治理与飞书内部身份定向回归
  `8 passed`，修复后暂停和恢复均完成真实接口验证。
- 首次主动自投递确认测试未生成确认卡片：用户已明确“给我发送”，模型仍追问收件人，
  同一 trace 的工具调用数为 0。根因是强制路由没有把“我”映射到可信主体本地用户，
  且系统提示仍引用旧的 `user_ids/text` 字段。现已改为使用实时目录 Schema 的
  `recipient_user_ids/title/markdown/idempotency_key`，并将自投递确定性映射到可信主体
  UUID；定向回归 `47 passed`，开发镜像重建后恢复为 1 个事件消费者，等待重测。
- 第二次主动自投递确认测试仍未生成确认卡片：收件人歧义已消失，但模型使用自然语言
  “发送飞书消息”搜索能力；目录当前按完整子串匹配，而摘要为“主动投递飞书消息”，
  因空结果误判 `dazah_tool` 不可用。直接以稳定 operation 搜索已验证可返回
  `identity.deliver_feishu_message`，现已强制该写操作按精确 operation 完成
  `search → describe → execute`，等待再次重测。
- 第三次主动自投递确认测试已进入 `dazah_tool`，但模型在 execute 阶段遗漏
  `operation`，收到稳定错误 `operation is required for execute`，仍未生成确认卡片。
  现已把明确发送请求的受信 operation 绑定到本轮运行上下文；仅允许
  `identity.deliver_feishu_message` 在模型遗漏 execute operation 时从可信上下文补全，
  其他操作继续失败关闭，等待再次重测。
- 第四次主动自投递确认测试仍返回相同 execute operation 错误。真实调度复现确认根因
  不在模型：工具注册器按统一约定把整份参数字典传给 handler，而 `dazah_tool` 被直接
  注册成关键字参数函数，导致运行态 `action` 收到字典并误入 execute 分支；此前测试
  绕过注册器所以未发现。现已增加注册器适配包装器，并补充真实
  `registry.dispatch` 回归，确保 action、operation、body 和 task_id 正确展开。
- 第五次主动自投递确认测试仍显示旧的 execute operation 错误，但运行日志证明该轮
  没有再次调用工具，而是复述了连续会话历史中的失败结果。根因是上游 Hermes 会复用
  会话首轮持久化的 system prompt，后来加入的本轮写操作动态路由在连续私聊中不会随
  `system_message` 更新。现已将该路由作为每轮独立的可信 system 消息注入，同时只把
  原始用户正文写入持久历史，并明确禁止复述旧工具错误。连续会话与注册器边界定向
  回归 `53 passed`。开发容器重启后以全新隔离会话真实调用 `/v2/agent/runs`，返回
  `200` 并生成 1 个 `identity.deliver_feishu_message / medium / pending` 确认项，
  未发生实际投递；Gateway 状态为 `connected` 且事件消费者数量为 1。等待在真实飞书
  当前私聊中验证确认卡片展示、原请求人点击、重复点击与最终投递回执。
- 第六次主动自投递已在真实飞书私聊生成原生确认卡片。后端确认记录与卡片确认单一致，
  operation 为 `identity.deliver_feishu_message`、状态为 `pending`、风险为 `medium`；
  脱敏校验确认请求人、可信主体和收件人为同一本地用户，确认快照包含 title、markdown
  与 idempotency_key。确认前 Hermes `delivery_outbox` 数量为 0，证明未提前投递。
  当前卡片仍把“影响数量”显示为 0，并以 `-` 展示风险原因和关键变更预览，需在治理卡片
  展示层补充面向主动投递的中文摘要，但不影响本轮确认安全边界。等待原请求人点击“允许”
  后验证执行、实际飞书回执和重复点击幂等。
- 第六次卡片首次点击未产生响应：后端确认继续为 `pending`，Hermes 投递队列仍为 0，
  证明没有静默执行。点击时间附近身份解析返回 `403`。根因是固定上游 Hermes 在卡片
  回调中把 P2P 来源错误标为 `group`，Dazah 因而误套群白名单。兼容边界现对已通过
  严格解析的确认按钮回调跳过不可靠的群 chat_id，只解析绑定用户；后端仍按确认单
  `user_id` 校验必须由原请求人操作，普通群消息继续执行群准入。回调身份、普通群准入、
  Gateway、渠道和工具定向回归 `55 passed`，Ruff 通过。修复加载完成时原确认单已达到
  五分钟有效期，按失败关闭重新生成确认单后继续验收。
- 第七次主动自投递确认执行已通过：原请求人点击新卡片“允许”后，后端确认状态变为
  `executed`，确认解析接口返回 `200`；Hermes 只创建 1 条投递记录，尝试次数为 1、
  状态为 `delivered`、无错误，飞书私聊实际收到独立的目标卡片消息。与此同时发现私有
  raw-card 兼容发送层丢弃 SDK 回执，导致该记录没有持久化外部 message_id。现已在唯一
  兼容边界将 SDK 响应归一化为 success/message_id/error，卡片投递与文本投递统一按
  真实结果完成或重试；Gateway、运行态、渠道、确认与回执定向回归 `79 passed`，Ruff
  通过。既有已送达记录无法追溯补写 message_id，下一次主动卡片投递继续验证真实回执。
  当前确认单保持 `executed`，等待重复点击验证不会产生第二条投递。
- 第七次卡片重复点击的幂等安全性已通过：第二次点击到达后端并返回 `409 Conflict`，
  确认仍为 `executed`，已执行确认数、投递记录数和总尝试次数均保持为 1，没有产生第二
  条业务消息。用户界面未显示冲突反馈，根因是上游把 card action token 用作普通消息
  reply target，错误回显不可靠。现已对确认回调通过公开普通消息发送独立、脱敏反馈：
  成功、拒绝、重复/过期、非原请求人和未知失败均使用稳定中文语义且不引用 action token。
  确认、回执、Gateway 与运行态定向回归 `81 passed`，Ruff 通过；等待再次点击已执行卡片
  验证“已处理或已过期，未重复执行”反馈可见，同时投递数继续保持不变。
- 重复点击反馈真实验收已通过：用户再次点击“允许”后收到“该确认已处理或已过期，未
  重复执行。”，没有新增业务投递。按用户要求，确认卡片过期时间现从后端 UTC ISO 时间
  转换为 `Asia/Shanghai`，展示格式为 `YYYY-MM-DD HH:mm:ss（北京时间）`；后端继续使用
  UTC 存储和比较，不改变过期语义。定向回归更新为 `82 passed`，Ruff 与 compileall
  通过，开发 Gateway 重启后为 `connected / 1 consumer`，运行态渲染校验将
  `2026-08-03T05:14:12.356118Z` 正确显示为 `2026-08-03 13:14:12（北京时间）`。
- 北京时间与 message_id 联合复测的首次请求未生成确认卡片：虽然明确结构化的自投递
  指令已被路由规则识别，但模型仍只输出确认声明且没有调用工具，安全校验随后替换为
  “没有查询到后端真实确认记录”，后端没有新增 confirmation，证明失败关闭有效。为消除
  写操作是否调用工具的模型随机性，明确包含当前用户、标题和内容的飞书自投递现改为
  确定性编排；Hermes 仍经实时 `dazah_tool search → describe → execute`，权限、风险、
  Schema 和 confirmation 继续完全由后端裁决。结构化字段只从本轮带引号的标题/内容提取，
  收件人只能取可信主体本地 UUID。确认、Gateway、运行态和回执定向回归更新为
  `84 passed`，Ruff 与 compileall 通过，开发 Gateway 为 `connected / 1 consumer`，
  等待再次真实复测北京时间卡片和外部 message_id 回执。
- 北京时间与 message_id 联合复测已通过：确定性自投递生成新的 `medium / pending`
  确认卡片，飞书真实显示 `2026-08-03 13:32:07（北京时间）`；确认前投递记录与尝试
  次数均未增加。原请求人点击“允许”后收到“操作已确认并执行。”和独立目标卡片，后端
  确认变为 `executed`。Hermes 新增且仅新增 1 条投递记录，状态 `delivered`、尝试次数
  1、无错误，并成功持久化非空飞书 message_id。旧记录没有回溯伪造 message_id；当前
  总投递 2 条、总尝试 2 次，与两次真实成功投递一致。下一项进入拒绝确认验收。
- 拒绝确认真实验收已通过：新确认卡片显示北京时间，原请求人点击“拒绝”后收到“操作
  已拒绝。”；后端确认状态为 `cancelled`、executed_at 为空，确认解析接口返回 `200`。
  Hermes 投递总数、delivered 数和总尝试次数均保持为 2，没有生成标题为拒绝验收的业务
  投递。下一项验证拒绝后再点击允许的状态冲突仍失败关闭。
- 同一已拒绝卡片再次点击“拒绝”的幂等测试已通过：用户收到稳定的“该确认已处理或已
  过期，未重复执行。”反馈，后端返回 `409 Conflict`，确认仍为 `cancelled` 且
  executed_at 为空；投递总数和总尝试次数仍为 2。仍需在同一卡片点击“允许”验证不同
  决策之间的状态冲突。
- 已拒绝卡片再点击“允许”的交叉状态冲突测试已通过：用户收到相同的“该确认已处理或
  已过期，未重复执行。”反馈，没有出现目标业务消息；后端本次允许返回 `409 Conflict`，
  确认继续为 `cancelled` 且无 executed_at。投递总数、delivered 数和总尝试次数均保持
  为 2，首次拒绝决策不可被后续允许覆盖。下一项进入真实过期点击验收。
- 确认过期真实验收已通过：确认卡片显示 `2026-08-03 13:41:59（北京时间）`，超过
  该时间点击“允许”后收到“该确认已处理或已过期，未重复执行。”，没有出现目标业务
  消息，Hermes 投递总数、delivered 数和总尝试次数均保持为 2。首次验收发现接口虽返回
  `409 Conflict`，但抛出的异常导致同事务内 `expired` 更新被回滚，数据库仍错误停留在
  `pending`；已将过期解析改为返回终态结果，由 Web/Gateway 路由返回 `409` 后正常提交，
  并补充服务与 Gateway 路由回归。定向测试 `23 passed`、Ruff 通过。开发后端重新加载后
  在同一卡片复点，数据库确认状态成功持久化为 `expired`、executed_at 为空，投递计数与
  尝试次数未增加。
- 高风险人工责任判断失败关闭验收已通过：私聊请求“重启仓储飞书 WebSocket”后，助手
  明确说明关键连接重启属于需要最终责任判断的高风险操作，要求在对应业务页面手动处理；
  没有生成确认卡片。后端没有新增确认，`warehouse.restart_feishu_ws` 工具审计状态为
  `rejected_by_policy`，未进入执行阶段。
- 主动纯文本投递真实验收已通过：补充 `identity.deliver_feishu_message` 的 `card/text`
  动态工具契约，文本投递只发送正文并禁止携带卡片按钮；飞书私聊中的显式“文本消息”
  请求会确定性生成 `message_form=text` 的中风险确认。后端定向测试 `32 passed`，Hermes
  定向测试 `68 passed`，两侧 Ruff 与编译检查通过。真实确认前投递基线为 2；原请求人
  点击“允许”后收到“操作已确认并执行。”及独立普通文本气泡，确认状态为 `executed`。
  Hermes 仅新增 1 条 `text / delivered` 投递，首次尝试成功、无错误并持久化非空飞书
  message_id；总投递、delivered 和总尝试次数均为 3。
- 相同幂等键真实 outbox 验收已通过：对上述已成功文本投递使用相同幂等键、收件目标和
  内容重新提交，Hermes 返回原 delivery_id，投递记录总数保持为 3，原记录继续为
  `delivered / attempts=1`；飞书侧确认没有再次出现第二个文本业务气泡。
- 可重试失败与最终失败真实链路已通过：通过 Hermes 内部投递 API 向明确无效的测试
  open_id 提交无业务内容文本探针，运行态依次观察到 `pending:0 → retry:1 → retry:2 →
  sending:3 → failed:3`。最终错误标记非空、message_id 为空，证明两次可重试失败后第三次
  进入最终失败，且没有投递到任何真实用户。
- 飞书 Base 第一轮真实验收已通过：私聊提供已共享的 Base 资源链接并要求只列数据表，
  助手成功解析资源并按工艺阶段列出 17 张真实数据表；没有出现 Scope、资源共享权限或
  token 错误。下一轮验证按表名复用 table_id 并执行 `record-list`。
- 飞书 Base 第二轮 `record-list` 真实验收已通过：同一私聊仅按名称指定“进料数据记录表”，
  助手没有再次索要链接或内部标识，成功复用第一轮上下文并通过 `base +record-list` 返回
  恰好前 3 条记录，同时明确仍有更多记录；回复标注真实数据源与北京时间，未泄露 Base
  token、table_id 或 record_id。下一项验证 `record-search` 与后续 `record-get`。
- 飞书 Base `record-search` 真实验收已通过：继续在同一私聊按“批次号”字段搜索精确
  关键词，助手通过 `base +record-search` 返回匹配数量 1 和指定的日期、批次号、物料名称，
  并显示筛选条件与北京时间；没有重新索要 Base 链接，也未泄露 Base token、table_id 或
  record_id。下一项验证从搜索结果复用内部 record_id 执行 `record-get`。
- 飞书 Base `record-get` 真实验收已通过：助手从上一轮唯一搜索结果复用内部 record_id，
  没有要求用户手工提供标识，通过 `base +record-get` 返回指定的日期、批次号、物料名称、
  温度、浓度和 pH 字段，并继续隐藏 Base token、table_id 与 record_id。至此私聊中的
  `table-list → record-list → record-search → record-get` 主链路完整通过。
- 飞书 Base 无效/未授权资源失败关闭验收已通过：首次请求被通用防伪逻辑将只读资源回复
  中的“已经执行”误判为 Dazah 写操作声明，错误返回“没有后端真实确认记录”。已调整为
  飞书原生资源直读请求不套用 Dazah 写确认校验，写操作防伪保持不变；定向测试
  `32 passed`，Ruff 与编译检查通过。开发 Gateway 恢复 `connected / 1 consumer` 后用
  同一测试链接复测，助手返回飞书稳定错误码 `91402 / NOTEXIST`，明确说明资源不存在或
  机器人没有访问权限，没有回退展示先前成功 Base 的数据，也未泄露 token。
- Web/飞书高风险策略一致性真实验收已通过：平台 Livzon 助手使用与飞书相同请求“重启
  仓储飞书 WebSocket”，返回同一人工责任判断拒绝语义，明确普通消息发送等可确认写操作
  不受该策略拦截；Web 未生成确认并在会话策略层直接失败关闭，飞书工具入口审计为
  `rejected_by_policy`，两端均未执行连接重启。
- Web→飞书中风险确认与结果语义一致性真实验收已通过：首次 Web 显式请求“文本消息”时
  模型仍生成 `message_form=card`，已取消该错误确认，并将可信主体自投递的确定性
  `card/text` 路由从飞书扩展到 Web；Hermes 定向回归 `87 passed`，Ruff 与编译检查通过。
  复测生成 `medium / pending / text` 新确认，旧 card 确认为 `cancelled`；确认执行后 Web
  显示已执行，后端状态为 `executed`。Hermes 仅新增 1 条 `text / delivered / attempts=1`
  投递，飞书 message_id 非空、无错误；飞书私聊真实收到单个普通文本气泡，没有卡片或
  重复消息。
- Web 会话历史恢复真实验收已通过：关闭助手、刷新平台页面并重新打开后，旧的错误 card
  确认及“已取消”状态、新的 text 确认及“已执行”状态均按原顺序恢复，没有重新生成确认。
  Hermes 投递计数保持总数 5、delivered 4、失败探针 1、累计尝试 7，证明历史恢复没有
  触发重复执行或重复投递。
- Web PDF 附件真实验收已通过：首次请求在后端完成安全文本提取后，内部 V2 请求错误复用
  原始上传附件 Schema，因缺少已主动移除的 `data_base64` 发生校验异常，前端只显示连接
  中断；同时发现会话归档后访问服务端更新时间字段触发 `MissingGreenlet`。已新增“已处理
  附件”内部契约（文档传提取文本、图片才传 base64），流管线异常改为稳定
  `agent.internal_error` 事件，并在归档后显式刷新会话。定向测试 `34 passed`、Ruff 通过，
  开发后端健康 `200`。复测成功读取 PDF 真实标题并返回不超过三点摘要，没有泄露本地或
  缓存路径；旧会话状态为 `archived`，新会话为 `active` 且恰好持久化一条 user 和一条
  assistant 消息。
- Web 停止生成首次真实验收发现持久化缺口：点击停止后界面立即结束流并保留当时已显示的
  部分文本，但刷新并重新打开会话后只恢复用户消息。原因是后端此前仅在收到 V2
  `finished` 时保存助手消息。现已在客户端取消流时以屏蔽取消的数据库收尾写入保存非空
  部分回复，并标记 `generation_status=stopped`；空回复、上游显式错误和已收到终态的请求
  不会写入或重复写入。定向测试 `20 passed`、Ruff 通过。开发环境真实复测在首段增量输出
  后停止，服务端会话为 2 条消息且只有 1 条助手消息；刷新页面后从历史会话继续对话，
  已停止的部分回复完整恢复，没有继续生成、完整回复替换或重复助手消息。
- Web 多确认项与部分成功真实验收已通过：同一会话连续创建 A、B 两个独立的飞书文本
  投递确认，页面同时展示两张不同有效期的中风险确认卡，创建阶段没有提前投递。仅执行 A
  后，后端 A 为 `executed` 且存在执行结果，B 仍为 `pending`；A 对应投递为
  `text / delivered / attempts=1` 且 message_id 非空，飞书私聊只收到一条 A 消息。
  随后取消 B，后端 B 为 `cancelled`、无执行时间和结果，飞书没有收到 B，证明确认项之间
  不会联动执行，单项成功与单项取消的部分结果语义正确。
- Web 确认过期首次真实验收通过前端安全表现：确认卡到达有效期后自动消失，飞书没有收到
  测试消息；但检查发现数据库状态仍为 `pending`，仅由前端按时间过滤，会造成治理页和审计
  的幽灵待确认。现已在用户会话恢复/轮询和管理员确认列表查询入口原子持久化到期记录为
  `expired`，并使用行锁避免并发重复处理。定向测试 `25 passed`、Ruff 通过。开发环境刷新
  后从历史会话继续该对话，数据库状态真实变为 `expired`，无执行时间和结果；Gateway
  保持 `connected / 1 consumer`、待投递为 0，卡片继续隐藏且飞书无消息，真实复测通过。
- Web 实际 90 秒超时提示与失败关闭已通过真实验收：在开发环境确认目标为
  `hermes-lite:dev` 后短暂停止 Hermes 进程，并设置自动恢复保护；平台请求等待满 90 秒后
  精确显示“回复超过 90 秒，请重试；系统已结束本次等待”，输入区恢复可用。随后 Hermes
  已恢复为健康 `200`，Gateway 为 `connected / 1 consumer`。该会话仅持久化 1 条用户消息，
  没有助手消息、确认或投递；Hermes 待确认和待投递均为 0，证明超时没有后台补执行。
  超时发生在 V2 `accepted` 前，因此前端没有取得 session_id；随后在同一界面重试会创建
  新后端会话，该恢复请求正常持久化 1 条用户消息和 1 条助手消息，没有确认或投递副作用。
  Gateway 持续为 `connected / 1 consumer`，超时后的后续请求恢复闭环通过。
- Web/飞书质量偏差只读一致性首次真实验收未通过：同一请求在 Web 产生真实
  `quality.list_deviations / succeeded` Tool Trace，并按数据库事实返回 0 条；飞书未产生
  该工具审计，却声称数据来源为该 operation 并返回 3 条不存在的偏差记录。数据库
  `quality.deviations` 当前真实数量为 0，因此确认飞书回复属于无工具证据业务数据。
  Hermes 已增加质量偏差列表的本轮强制动态搜索/描述/执行路由，并增加通用证据门：回复
  声称 Dazah 数据来源或查询结果时，声明的 operation 必须存在于本轮 `ok=true` Tool Trace，
  否则失败关闭且不展示记录。定向测试 `36 passed`、Ruff 通过；开发 Gateway 已恢复
  `connected / 1 consumer`。飞书复测产生真实 `channel=feishu / succeeded / ok=true`
  Tool Trace，并按数据库事实返回总计 0 条；Web 同请求的 Tool Trace 同样为
  `channel=web / succeeded / ok=true`，两端均不生成确认、不泄露内部 ID，只读结果语义
  一致性真实验收通过。
- Web/飞书质量 Scope 失败关闭开始真实验收：经用户授权，临时将当前账号 Agent Scope 中
  76 个 `quality.*` 工具移除，其他模块和常规页面权限不变，并记录测试审计。Web 复测正确
  通过能力目录防枚举返回“未找到质量相关可用操作”，未展示记录或 Schema、未生成确认。
  用户提供的飞书截图对应临时变更前的旧查询；数据库在临时窗口内没有新增飞书
  `quality.list_deviations` Tool Trace，因此飞书侧尚未形成有效证据，不能判定为绕过权限。
  为避免持续影响使用，质量 Agent Scope 已通过正式同步恢复为 76 个工具、版本 8、状态
  `synced`，并补充恢复审计。下一轮应使用带唯一验收标记的新请求重新验证飞书失败关闭。
- 飞书质量 Scope 第二次唯一标记复测发现跨轮证据污染：Scope 已临时移除全部
  `quality.*` 能力且本轮后端没有新增工具调用，但助手仍复用了旧轮次的成功
  `quality.list_deviations` Tool Trace，输出了“Dazah 平台查询结果”。将该回复单独传入现有
  证据门会被正确拦截，确认根因是 Agent 会话返回的累计 `tool_trace` 被误当成本轮可信证据。
  Hermes 现按 runtime task 在 Dazah 工具网关内独立采集本轮真实 `execute` 调用，并在
  Agent 返回后无条件替换模型或会话携带的轨迹；轨迹只保留 operation、成功状态、确认和
  投递所需最小字段，不复制业务结果或原始外部错误。伪造旧成功轨迹的回归已覆盖，定向测试
  `37 passed`、Ruff 与 compileall 通过；本机全量测试因开发虚拟环境缺少 `pypdf` 在收集
  阶段阻断，运行容器也未安装 pytest，未将其记为全量通过。质量 Scope 先恢复到版本 10，
  第三次唯一标记复测仍返回旧结果，且回复内查询时间停留在 `14:16`，证明 Windows bind
  mount 中源码虽已更新，但运行进程未可靠热加载本次修复。该轮没有被记为修复后验收；
  Scope 已立即恢复到版本 12。随后显式重启 `hermes-lite:dev`，确认源码挂载不变、健康状态
  正常、Gateway 为 `connected / 1 consumer`、待投递为 0。再次临时移除质量能力为版本
  13 后，第四次唯一标记 `20260803-04` 已命中新进程并正确失败关闭：助手明确说明没有取得
  本轮真实工具查询结果，不展示任何业务记录；该请求与助手回复均已持久化，本轮新增
  `quality.*` 工具调用数为 0、确认数为 0、助手证据来源数为 0。测试结束后质量 Scope 已
  通过正式同步恢复到版本 14，`quality.*` 工具 76 项、状态 `synced`，并记录恢复审计。
- 能力目录紧急禁用验收开始前发现执行门禁缺口：现有 `admin_enabled/status` 只约束能力
  搜索与详情，已知稳定 operation 的普通执行、自动化执行和待确认执行仍可绕过目录状态。
  后端现将同步目录状态加入统一 `ToolExecutor`，禁用能力的直接 execute 稳定返回 `409 /
  Agent tool is currently disabled`，所有全局 Registry 执行入口共用该门禁；隔离测试 Registry
  不参与平台管理目录。目录与执行器定向回归 `14 passed`、Ruff 通过；后端开发容器已显式
  重启并健康，运行态已知 operation 探针真实返回 409。临时禁用
  `quality.list_deviations` 前的影响预检为自动化 0、Skill 0、Workflow 0。Web 首次使用通用
  “质量偏差”语义时改走仍启用且语义重叠的 `quality.list_deviation_report_records` 并成功
  返回 0 条；被禁用 operation 调用数仍为 0，因此该轮只证明单 operation 门禁，不代表整类
  业务意图被禁用。随后 Web 与飞书分别使用唯一标记并明确限定仅可调用
  `quality.list_deviations`，两端均返回“没有取得本轮真实工具查询结果”，不展示业务记录，
  服务端两轮工具调用数和确认数均为 0；目录精确搜索结果也为 0。完成后能力已恢复为
  `active` 并记录审计，运行态精确搜索恢复为 1 条且直接只读执行 `ok=true`。本项真实验收
  通过，同时确认治理页面应提示“禁用单一 operation 不等于禁用所有语义相近能力”。
- 已生成确认在紧急禁用后的执行拦截真实验收通过：Web 先生成 1 个
  `identity.deliver_feishu_message / medium / pending` 确认，创建阶段无投递；随后临时禁用
  operation，影响预检为自动化 0、Skill 0、Workflow 0。点击“确认执行”后统一执行器返回
  409，确认没有执行，Hermes 投递总数保持 6、累计尝试保持 8，飞书没有收到测试业务消息。
  首次界面直接展示英文 `Agent tool is currently disabled`，现已改为稳定中文“该能力已被
  管理员停用，当前无法执行”，定向回归仍为 `14 passed`、Ruff 通过，后端开发容器显式
  重启后健康。能力已恢复为 `active`；因 409 拒绝后原确认仍为 `pending`，为防止恢复后旧卡
  重新执行，本次测试确认已通过正式取消流程置为 `cancelled` 并记录审计。治理页禁用影响
  仍需把待确认项纳入展示，并提供取消/失效处置选项。
- 能力目录详情抽屉真实视觉检查已确认权限键、超时、幂等、受影响自动化和输入 Schema
  正常展示，同时发现 `quality.list_deviations` 输出 Schema 为空对象，无法支撑契约治理。
  现已为该 operation 注册分页输出模型，明确 `items/total/page/page_size` 及偏差列表项字段；
  目录同步后运行态为 `active / output type=object`，四个顶层属性已落库。相关后端测试
  `33 passed`；新增代码的 Ruff 语法、导入与完整受影响文件检查通过。质量 Agent 工具文件
  全量 Ruff 仍有 53 条既有 E501 行长债务，本次未扩大范围批量格式化。等待前端刷新后复核
  输出 Schema，并补看抽屉顶部状态、版本、风险和读写字段。
- 继续抽查确认上述缺口覆盖全目录：146 个能力中原先仅 1 个显式输出 Schema，145 个为空。
  注册器现从 handler 返回注解生成非空 JSON Schema 基线并写入来源扩展；目录同步后
  `active=146 / missing=0 / explicit=1 / inferred=145`。为避免把通用 `dict[str, Any]`
  误认为完整字段契约，详情抽屉新增“字段级契约”与“通用契约 · 待细化”标签，推导契约
  展示警告；同时补齐摘要、模块、版本、状态、读写、风险和人工确认字段。后端目录/执行器/
  质量工具定向回归 `32 passed`，前端 typecheck 与 lint 通过，治理页 E2E `5 passed`。
- 能力目录模块筛选原先从当前分页结果临时生成选项，导致用户只能看到当前页涉及的模块。
  现改为从完整能力目录独立加载、去重和排序模块选项，并将无模块归属的内建能力统一展示为
  `platform`；后端同时补齐 `module=platform` 对数据库空模块的筛选语义。模块选择器增加可访问名称，
  E2E 固定验证 `energy/platform/procurement/quality/warehouse` 五类均存在，不再受当前页影响。
  前端开发容器已显式重启，确认 `NODE_ENV=development`、项目源码挂载到 `/app`，访问状态
  为登录跳转 `307`。字段级业务输出模型仍仅 1 项，其余 145 项作为逐模块契约治理待办，
  页面不会再以空对象或“完整契约”误导管理员。
- 首次私聊发现 Gateway 接入租户别名被误作本地授权租户，导致持久会话准备 `403`；
  已改为外部绑定按接入租户解析、可信主体按本地用户租户授权，定向回归 `4 passed`。
  群聊测试按用户要求暂不执行。
- 飞书私聊表格附件读取已补齐 `xlsx/xls/csv`：XLSX 支持多工作表并限制解压总体积，
  XLS 使用旧版工作簿读取器，CSV 支持 UTF-8 BOM 与 GB18030；三类格式统一限制工作表、
  行、列和总字符数，提取内容仅进入当前用户消息上下文且不展示本地缓存路径。Hermes
  附件与 Gateway 定向回归 `43 passed`、全量回归 `115 passed`，Ruff 通过；仍需使用真实
  飞书私聊分别上传三种格式完成运行态验收。

- 助手、采购和 Livzon Agent 设置关键 E2E：`13 passed`；其中助手新增停止生成、
  附件与历史、多确认/过期/部分成功、断线恢复四条用例。
- 飞书连续会话已写入平台 `agent_sessions/agent_messages`，Gateway 重启后可恢复历史，
  外部 `message_id` 重复请求跨进程去重；引用正文仅保留为冷启动兜底。
- Trace 已关联飞书入站/回复、工具、确认、领域事件和投递，并提供不含业务正文或
  凭证的 JSON 导出与 SHA-256 摘要。
- 身份目录同步、来源/验证时间、租户隔离、部门/最近活动筛选和冲突工作台已完成；
  冲突不会自动覆盖既有绑定。
- 运行总览已展示事件消费者、消费者数量、最近配置变更和异常 Trace 跳转。
- 后端 `.venv` 已恢复；根目录 OpenAPI/前端类型生成成功。
- 后端 `898 passed`，覆盖率 lines `62.71%`、branches `33.75%`；迁移在独立
  `dazah_test` 完成 downgrade/upgrade 往返并回到 `7a31c9e4d2b8 (head)`。
- Hermes-Lite `84 passed`；前端 unit `20 passed`、关键 E2E `13 passed`、
  typecheck/lint/build 通过。
- 前端、后端和 Hermes 开发 Compose 镜像均构建成功；运行态冒烟为前端设置入口
  `307`（未登录跳转）、后端健康 `200`、新增治理路由未认证 `401`、Hermes 健康
  `200`。前端运行容器保持 `NODE_ENV=development` 与源码 bind mount。
- Mypy 仍受仓库跨模块既有类型债务阻挡（本次全目录运行共 205 项）；未将其误记为
  本次功能回归。工作区尚未形成提交，因此 test-impact/diff coverage 不能对当前
  未提交差异给出有效结论。
