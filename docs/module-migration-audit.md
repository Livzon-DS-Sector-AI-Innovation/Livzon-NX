# 五模块迁移审查与交付记录

## 1. 范围与基线

- 迁移基线为 `C:\Users\Dan\Documents\dazah-Migration\`；其说明文档、配置和上传附件仅作实现参考，不改变本次用户请求。
- 范围为质量、注册、人事、仓储和系统权限。认证、对象存储、LLM、Agent、MCP、调度、WebSocket 及现有独有业务能力继续保留。
- `origin/dev` 已以普通 `--no-ff` merge 纳入当前分支，基线 merge commit 为 `4f09d10`；未使用 rebase、强推或覆盖远端历史。
- 未提交的 LLM/Hermes 用户改动已纳入本地提交 `0491133`，与迁移改动一起交付；未复制源目录约 2,513 个业务附件。
- 系统权限五页已放入“系统设置 → 权限与数据范围”；`/system/*` 仍保留为兼容直达入口，前端展示过滤不替代后端鉴权。

## 2. 页面—接口—权限—数据—上传—测试矩阵

| 模块 | 页面/关键闭环 | 后端权限与数据范围 | 主要数据/兼容表 | 上传与文件授权 | 已补测试 |
| --- | --- | --- | --- | --- | --- |
| 质量 | 文件目录、偏差/CAPA、OOS/OOT、验证、检验物品/仪器/成品、飞书同步 | `quality:read/write`；后端强制校验数据范围 | 文档、偏差/CAPA、OOS/OOT、验证、检验和飞书读镜像 | MIME 实测、扩展名、大小、数量、文件名、UUID 存储键；失败补偿 | AsyncClient 路由/权限、上传安全、CAPA、OOS/OOT、质量客户端全量读路径、空数据页面和导入契约 |
| 注册 | 项目、证书、费用、知识库、参比制剂、补充资料、授权信、验证审计 | 模块读写；带部门/负责人归属的实体叠加数据范围 | 注册台账、证书、费用、知识库、验证审计和旧兼容字段 | 先校验后上传；`/download` 受鉴权；失败清理孤儿对象 | 下载、导入、知识库、注册 API、纯映射和空数据页面 |
| 人事 | 员工、入离职、新厂五页、招聘、合同、培训、ESG、设置；考勤入口隐藏并明确未迁移 | `hr:read/write`；部门别名和归属查询/写入接入数据范围 | 员工、合同、招聘、培训及 legacy 入离职表 | 合同/培训附件按记录和权限读取；MIME 白名单与补偿删除 | 新旧厂区接口、候选人通知、权限输入校验、服务端 API、培训空首屏 |
| 仓储 | 原辅包材、五金、成品、飞书配置、页面映射、分析、AI | `warehouse:read/write` 及 product/hardware/raw 精细权限 | 物料页快照/行表、Feishu root/table/page binding/analysis 表 | 配置不回显密钥；记录/字段/附件下载授权；写入走 Server Actions | 页面、主表筛选/分组/详情/编辑交互、配置契约、查询参数、飞书 Server API、空数据表格 |
| 系统权限 | 角色、用户角色、部门角色、菜单、权限验证 | `identity:admin`；`module.view` 只映射读权限 | RBAC、菜单、角色菜单、部门规则、审计 | 不涉及业务上传；变更审计并失效权限缓存 | 五页设置入口、Action 全量请求构造、权限页面渲染和纯菜单树 |

## 3. 兼容接口收口

迁移前要求保留的 52 个重点操作已恢复并使用现有 Service、Repository、权限和审计链路，不复制重复业务表：

- 人事 18 个：`onboarding-records`、`departure-records`、`new/*`、流动分析、培训通知及候选人通知。
- 质量 12 个：旧 OOS/OOT 记录、限度产品/项目查询修改删除和飞书同步。
- 仓储 22 个：飞书配置测试、根节点、WS 状态、`page-data`、记录/附件、分析配置/运行/查询。

已修复的前端断链包括 CAPA 三项操作、注册受鉴权下载、离职证明 `/certificate`、HR 新厂 Server API、仓储配置写入和页面映射发布。迁移模块实际调用的 API 已通过生成 OpenAPI 路径检查；`remark-gfm` 已在本地和开发依赖卷中可解析。

## 4. 权限、上传与 AI 边界

- 后端是最终权限边界；角色、菜单、用户角色和数据范围变更写审计并触发权限缓存失效。
- 上传拒绝路径穿越、控制字符、超长名称、错误扩展名、伪造 MIME、超限文件和超数量；数据库或对象存储任一步失败均执行补偿。
- 业务 AI 统一经过 `app.core.llm.llm_client`，区分配置缺失、限流、无效输出、超时和供应商失败；不向前端返回密钥、完整 prompt 或原始供应商响应。
- Agent/Hermes 继续使用独立治理、白名单、确认策略和权限隔离；普通业务模块不绕过 Agent 入口。

## 5. 数据库与生成物

- 仅新增当前 head 链上的修复 migration：`c1e7f4a9b2d6_repair_production_feishu_read_mirrors.py`，父链为 `b5f4c8d1a2e3`。
- 修复 migration 只在缺失时创建生产 Feishu 读镜像表，未执行 DROP、数据清理或源 migration 复制；开发和测试数据库已升级到该 head。
- API 端点/schema 变更后由 `scripts/generate-api.ps1` 生成 OpenAPI、前端快照和类型；交付前核对重复 operation ID 与重复 method/path 均为零。

## 6. 当前验证与剩余门禁

已验证：

- 后端全量 Pytest：`2165 passed, 25 skipped`；覆盖率 XML 为 62.97% 行、39.53% 分支，达到全仓 floor 60%/33.5%。
- 后端迁移变更行覆盖（`origin/dev..HEAD`）：`53.11% (21800/41044)`，低于 80% 差异覆盖门槛，仍需继续补真实行为测试。
- 前端全量测试：`164 files passed, 683 tests passed`；覆盖率为 25.09% 行、18.60% 分支、21.45% 函数。迁移变更行覆盖（`origin/dev..HEAD`，使用当前工作树生成的覆盖数据）提升至 `24.31% (3876/15942)`，仍低于 80% 差异覆盖门槛；新增测试已覆盖 Server API/Action 认证契约、权限页面、质量客户端、仓储主表交互、空数据页面和核心纯函数，但仍不能替代全部页面行为测试。
- 受影响范围的 Ruff、迁移模块/LLM/身份平台 Mypy（`389 source files`）、Alembic `heads/check`、OpenAPI 重复操作检查、Hermes-Lite 既有回归、前端 lint/typecheck/build 均已执行并通过；全仓 Mypy 仍有 `2124 errors in 119 files`，主要集中在历史 production 模块及其测试，不能标记为通过。

仍需在推送前完成：

1. 前后端继续补充迁移变更行真实成功、失败、403、数据范围、上传回滚和危险操作测试；当前后端为 53.11%、前端为 24.31%，两套差异覆盖门禁均未达到 80%，不得通过排除文件或降低阈值。
2. 重新生成并核对 OpenAPI、前端生成类型和契约快照；运行 `check-test-impact.py --base origin/dev --head HEAD`。
3. 清理本地测试临时目录，确认 `git diff --check`、敏感信息扫描、状态和提交范围无误后，才可推送当前分支；不使用 force push。

本报告不把“文件存在”“菜单字符串存在”或 mock API 通过误写成完整业务验收；差异覆盖和真实后端/开发环境写操作仍是交付判断依据。
