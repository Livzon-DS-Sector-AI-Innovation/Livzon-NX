# 页面权限矩阵实施状态（2026-08-31）

## 交付边界

本次是现有身份体系上的增量实现，尚未完成整份页面权限计划的发布验收。
首批 HR、仓储、质量、采购保持草稿；不得手动修改数据库为已发布。
普通用户没有从旧角色或模块授权自动回填页面授权。旧事实没有删除。

### 核心框架收尾（2026-08-31）

本轮按用户要求暂停业务模块接入，完成核心生命周期与并发安全收尾。
规范与验收证据见 [页面权限核心规则](page-permission-core.md)。

- 新增独立的 230 页面生命周期登记簿及 CI 历史检查；退役标识留存、不复用，
  页面重做/归属或路由变化使用新标识，高风险动作删除须登记退役，新动作默认不授权。
  运行时和发布门禁也拒绝无效目录，不只依赖开发约定。中文改名与暂时停用不等于功能重做。
- 菜单标识不可改写；已删除菜单不能以旧标识重新创建，明确返回 409。
- 管理写入先串行化并重新核验操作者；用户、角色、发布记录加锁后刷新旧会话值。
  授权批量版本使用数据库原子自增；旧角色删除、权限和菜单绑定变更补齐 outbox/版本失效。
- 旧角色权限、菜单绑定和删除入口补齐受委托管理员的自身角色修改限制。
  核心回归覆盖并发保存、互相降级、过期操作者、发布预览过期、回退冲突及审计失败回滚。
- Livzon 快照摘要纳入实际页面策略、接口绑定和退役目录，变更后旧快照立即拒绝执行，
  部署更新后需要重新同步权限快照；没有改变 Hermes 动态工具契约或业务白名单。
- 后端 204 项通过、4 项原有条件跳过；前端 34 项、Hermes 6 项通过。
  Ruff、10 文件 Mypy、TypeScript、生命周期目录检查通过；根 API 生成前后哈希不变。
  本轮没有新增 migration 或环境变量，没有发布模块、部署或重做业务功能。

### 最新需求覆盖（2026-08-31）

- 原“管理员”和“超级管理员”统一显示为“系统管理员”，具有全部平台及业务权限。内部保留 `admin` 账号值和 `super_admin` 历史角色编码以兼容旧数据，不再向用户呈现两种管理员身份。
- 新迁移 `f7b2d9a4c103` 将历史手动超级管理员归并为管理员账号、更新角色中文名称、递增相关用户授权版本；没有回填普通用户页面授权。开发库已升级，独立测试库完成升级/降级/再升级。降级不会自动撤销管理员身份或降低授权版本，避免覆盖后续人工决定及锁死账号。
- 角色分配/移除与账号管理身份同步，降级同时撤销历史通配角色绑定；保留禁止修改自己身份和最后管理员保护。管理员列表会显示管理员身份，即使没有历史手动角色绑定。飞书登录保留已经明确分配的管理员身份。
- 管理员的页面矩阵覆盖不限制全权限，界面只读提示，后端拒绝创建无效覆盖。旧权限缓存不能阻止管理员访问，普通用户不能继续使用降级前的通配缓存；已变更版本的 Livzon 快照拒绝执行。
- 系统管理员拥有全部已注册 Livzon 工具权限，但自动编排仍遵守 `workflow_allowed` 和人工责任限制，确认与业务状态校验没有被当作权限取消。普通用户的未发布、未绑定页面工具仍失败关闭。
- 页面数据范围按“隐藏配置入口、保留存储及后端校验”处理：用户/角色矩阵列、差异说明、验证台和旧用户部门配置隐藏，不加载隐藏的用户部门编辑数据，也不随角色保存修改范围规则。已有页面范围随授权保存原样保留；没有把普通用户自动扩大为全部部门。
- 有独立高风险动作的页面默认展开；无动作页面不显示展开按钮。展开不勾选权限，仍需管理员明确选择。

以上最新要求覆盖历史记录中的“普通管理员不享有业务通配”和“超级管理员为独立身份”；首批四模块完整接入及发布验收仍未完成。

本轮验证：

- 开发容器使用独立 `dazah_test` 执行身份目录、Agent 页面/快照、模块访问和登录回归：185 项通过，4 项原有条件跳过。追加管理员业务上下文、HR 员工、采购及仓储页面回归：45 项通过。普通用户限制测试改用普通用户身份，管理员全权限由独立用例覆盖，未降低普通用户鉴权要求。
- Vitest 7 文件、31 项通过；包括自动展开但不勾选高风险动作、隐藏范围原值随保存保留、管理员全权限及降级后拒绝、直接 URL 校验。TypeScript、定向 ESLint 通过；UserRoleManager 的一项既有 hooks 依赖警告保留，没有新增警告。
- Next.js 构建通过；在本地 standalone 构建配合 mock API 执行 Playwright Chromium `e2e/identity/page-permissions.spec.ts`：4 项通过，验证模块中文、默认展开、隐藏范围、差异确认、409 编辑保留及普通用户页面拦截。测试服务随测试停止，没有部署生产环境。
- 定向 Ruff、触达后端文件 Mypy、Alembic 单 head、增量 migration 的测试库升级/降级/再升级、四模块菜单目录 `--check`、Hermes 动态工具契约 6 项测试通过。`git diff --check` 和包含当前未提交/未跟踪改动的测试影响检查通过（缺失测试组及未分类路径均为 0）。
- 本轮新增身份数据 migration，没有变更 HTTP 请求/响应 Schema、环境变量或手工修改生成文件，因此没有重复生成 API 契约。未执行全量测试覆盖率或首批所有页面的完整发布验收，不代表原计划全部完成。生产部署前仍需应用新增 migration；迁移回退不会自动撤销已经合并的管理员身份。

## 已实现

- 新增角色页面授权、用户页面覆盖、模块发布状态及角色授权版本结构。
- 角色权限并集、用户完整覆盖、空授权明确拒绝、访问/查询/操作递进联动。
- 指定部门保存稳定 ID；角色的指定部门与本人部门取并集；空范围不再跳过 SQL 过滤。
- 用户、角色页面配置接口，中文页面矩阵、角色来源、恢复继承、批量设置、调整原因。
- 所有模块名称复用导航中文名称（生产、设备、能源、安全、研发、注册、行政等），不显示原始模块代码。
- 用户/角色共用中文差异预览与二次确认；递进取消权限、模块批量设置、恢复真实角色基线、未保存提醒、版本冲突保留本地调整。
- 页面权限保存、发布和回退使用可序列化失败结果传递预期 4xx，避免生产 Server Action 将版本冲突隐藏成通用错误。
- 权限清单 CSV 改为中文菜单页面、业务动作、范围、角色来源与发布状态；不再导出原接口权限码，用户可控单元格进行公式转义。
- `/me` 页面授权摘要、菜单过滤、页面无访问/无查询状态、请求页面上下文透传。
- 版本化发布预览、原因与确认、紧急回退入口；预览摘要包含用户版本及有效授权事实。
- 接口强制校验精确路由模板、请求方法与页面绑定；未登记、重复或无范围适配的绑定拒绝。
- 直接用户授权和本人所属角色授权的自我修改限制；超级管理员保护的基础校验。
- 普通管理员不能调整命中本人部门的角色映射，也不能通过个人资料接口修改本人的授权关联部门。
- 用户资料更新对授权关联字段（部门、账号状态、管理角色）加行锁；实际变化才递增授权版本，记录审计和 outbox，提交后清权限及范围缓存。仅修改姓名或提交未变化的部门不会使快照失效。
- Livzon 工具增加页面和高风险动作元数据，平台通过注册的目录投影读取，未引入第二套 Hermes 白名单。
- 新增可复现的首批菜单生成脚本 `dazah-frontend/scripts/generate-page-menu.mjs` 与 `--check`；第一批菜单种子与前端静态菜单同步，已有数据库菜单设置不覆盖。
- 有效授权实时核验数据库菜单的类型、路由、状态与祖先；停用、删除、孤立、循环关系均拒绝。旧目录与新页面结构不一致时列入发布缺口，不自动替用户修改菜单。
- 采购接口已完成精确路由登记：申请按类别和部门限制列表、总数、详情、新建、更新、删除、导入与订单；合同按页面类别隔离。无可靠部门维度的供应商、发票、物料和合同页面明确“不适用”。
- 采购审批身份取自登录用户并校验页面审批岗位；工具执行实时读取页面权限并传递同一页面/部门上下文，结束后恢复上下文。合同工具不能跨类别查询或生成。
- 部门名称与未授权部门 ID 重名时拒绝；缺失或失效部门 ID 不通过名称兜底扩权；部门树循环不会无限递归。
- Livzon 执行入口拒绝旧授权版本、旧工具目录版本和未同步快照，不在执行时自动重建放行。
- 未发布模块的业务工具不再从旧模块授权派生，执行入口同样明确拒绝；快照摘要包含页面策略版本，旧快照需要重新同步。平台页面草稿兼容不受此规则影响，平台通用工具仍遵守原职责限制。
- 前端代理在业务 Server Component 执行前验证发布页面；无访问/无查询均明确拒绝，模块根入口转到授权页面。供应商覆盖导入增加单独授权入口控制和覆盖确认。
- 页面授权和模块发布审计归入统一“权限”分类。契约生成脚本修正原生子命令失败检测，并直接运行原有前端生成脚本以避免触发 pnpm 自动重装。

主要代码：`page_policy.py`、`page_permissions.py`、`page_permission_repository.py`、
`rbac_api.py`、`deps.py`、`data_scope.py`；前端用户与角色页面权限抽屉、权限模拟和模块发布组件。

## 必须完成后才能发布

1. HR、仓储、质量尚未登记完整 API 策略与可靠范围适配器。当前 OpenAPI 分别包含 206、40、294 个路径（不是方法数）；HR 已登记员工档案列表、统计、工号查询、详情、创建、修改、删除、单条同步（8 个方法绑定），仓储已登记物料页列表及记录详情/修改/删除（4 个方法绑定），质量已登记本地偏差台账列表、详情、关联 CAPA、创建、修改、删除、导出、报告人选择和批量删除（9 个方法绑定），其余路径仍有缺口。不能用路径名猜测批量登记或移除发布门禁。采购接口目录契约检查已通过，但不等于四模块整体发布验收完成。
2. 采购以外页面的部门策略仍属草稿；必须逐页核验列表、统计、详情、写入、附件、导入导出和外部数据。仓储镜像/质量通用数据页还需明确哪些记录具有可靠部门维度；不能将未知能力伪装成已适配。
3. 静态目录已将旧 `hr:employee-management` 作为退役目录身份，并以 `hr:employee-management:profile`、`hr:employee-management:feishu-contacts` 作为叶子页面。实际数据库菜单仍须在发布前通过菜单状态/结构缺口检查；不得修改历史授权或停用状态来强行通过。
4. 已发布请求在全局中间件绕过旧 RBAC 业务判定，再由模块依赖校验精确页面策略；其余模块内部旧细分权限判断仍需逐个衔接。
5. 未接入模块的 Agent 工具仍需逐工具映射、范围与职责验证。未发布页面工具已经失败关闭；首批保持草稿期间，相关 Livzon 业务工具将不可用，不能通过旧模块授权恢复。发布门禁通过后再同步新的工具快照。
6. 供应商覆盖导入及仓储物料页的编辑/删除/同步入口已接入，其他业务页面按钮仍需接入。服务端渲染前拦截、矩阵差异确认和冲突保留已验证，但完整发布/回退、全部业务动作 E2E 尚未完成。
7. 核心自我授权、管理员保护、部门映射和并发授权/发布已完成本轮专项回归（见核心规则）；后续新业务入口及外部并发副作用仍需按各模块单独验证，不能用核心通过替代业务验收。
8. 部分高风险动作中文标签仍由菜单启发式生成，需要逐页审核；高风险责任资格、二次确认及业务持久审计也需要完整验收，不能将请求日志当作全部业务审计。

## 验证记录

### 报告人联动与原子批量删除（2026-08-31）

- 补齐新增台账的报告人选择，显示中文姓名和部门，选择后自动关联只读部门；搜索防抖、失败重试、提交失败保留表单、重复提交锁和授权变化后清除选择均已接入。不恢复数据范围配置入口。
- 新增报告人选项接口，要求台账查询权限，只返回人员标识、姓名和部门；先按服务端范围过滤再搜索、分页及计数。前端最多显示 50 人，接口每页最多 100 人；缺失必要字段或同一人员标识存在多个联系人映射时不可选择，避免通过局部目录掩盖歧义。
- 创建时重新精确查验报告人，不再只从前 1000 条联系人中查找；强制页面权限下校验报告人部门范围及提交部门一致性，跨部门或部门已变化时拒绝，不信任表单选择。目录超时和上游失败提供中文错误，不返回上游敏感详情。沿用现有飞书全量读取适配器，目录规模较大时的缓存与性能优化仍待后续评估。
- 批量删除登记“操作 + 删除偏差记录”独立子权限。每批 1–100 个唯一 UUID，整批按稳定顺序锁定记录，完成全部存在性及范围校验后才写入；记录软删除和逐条业务审计在同一事务提交。混入无权/已删除记录或任一审计失败时没有部分成功。前端二次确认说明整批规则，提交期间阻止重复请求。
- 修正新增响应契约为实际返回的记录 ID 和编号，批量请求/响应及报告人目录使用明确 Schema；从根目录执行 `scripts/generate-api.ps1` 同步 OpenAPI 和前端生成类型，未手工修改生成产物。
- 验证命令：开发容器独立测试库执行 `pytest` 质量页面权限、目录绑定、服务迁移、兼容路由、Repository、关联 CAPA、导入导出规则及页面目录强制校验 8 个文件，48 项通过；`vitest run` 台账、报告人客户端、偏差/质量共享 Actions、菜单、代理、身份缓存 7 文件，38 项通过；`playwright test e2e/identity/page-permissions.spec.ts --project=chromium` 9 项通过，包含新增及批量删除完整浏览器流程。飞书和 E2E 上游均为 mock，未写真实业务记录。
- 定向 `ruff check`、`ruff format --check`、6 个源文件 `mypy --follow-imports=silent`、`tsc --noEmit`、Next.js 构建、四模块页面目录 `--check` 通过。定向 ESLint 零错误，保留既有列表/Actions 警告，未降低门禁。测试影响检查同时覆盖未提交和未跟踪文件；缺失测试组和未分类路径均为 0。
- 开发库已确认在 `f7b2d9a4c103` 单 head。本轮未新增 migration 或环境变量，未重复迁移升降级、全量覆盖率、Hermes 回归或生产部署；没有改变 Agent 工具注册或 Hermes 契约。批量导入、审批/调查流转、飞书报告记录及其余页面仍待接入，首批四模块继续草稿。

### 偏差台账基础接口与工具闭环（2026-08-31）

- 本地偏差台账以 `quality:deviations:deviation-ledger` 为最小页面，登记 7 个精确方法绑定。删除与导出分别要求独立子权限，中文动作明确为“删除偏差记录”“导出偏差台账”。没有启用“仅本人”，没有恢复数据范围配置入口。
- 已发布页面的列表、详情、关联 CAPA、创建、修改和删除共用服务端部门范围。显式跨部门筛选、修改范围外记录、将记录移到范围外/空部门均为 403；新增先验范围再查询飞书联系人，更新/删除使用行锁。列表和关联 CAPA 不暴露范围外 CAPA 的 ID、编号及明细。
- 普通修改禁止改写审批意见、审批状态、最终编号、退回步骤、关闭状态和流程记录。详情页的关闭选择只读，不再由普通保存提交状态变更；审批职责、审批入口和完整状态流转仍待独立接入。
- 导出复用列表的全部筛选和范围规则，修复原先忽略扩展筛选的问题；超过一万条明确要求缩小范围，不静默截断。新增/修改/导出写业务审计，删除保留原有软删除审计并补页面上下文。
- Livzon 的偏差列表、详情、关联 CAPA、创建和修改 5 个工具绑定真实叶子页面。真实服务回归复现旧 `quality_management` 实现未过滤范围的问题后，改为调用与 HTTP 相同的 `quality_deviation` 服务；写工具仍需确认，未添加 Hermes 静态白名单。
- 前端台账与详情按查询、操作和删除/导出子权限控制请求和入口；无查询时不取数且清理展示数据。查询缓存包含身份、授权版本和有效页面授权；授权版本经 `/me` 更新后重新取数，避免复用旧范围缓存。新建和 UUID 详情 URL 明确归属于台账，不扩权到报告记录、调查推送或 AI 子页。
- 本阶段结束时，批量删除与导入尚未具备整批校验，新增台账表单尚未传服务端必填的 `reporter_open_id`；其中报告人联动和原子批量删除已由上方后续验证记录补齐。批量导入、飞书报告记录、调查推送、审批、完整联系人管理及 AI 子页仍有发布缺口，不据局部完成宣称质量模块可发布。
- 验证：独立测试库中的质量基础服务、范围 CRUD/导出/工具、工具适配器与页面目录等 13 个定向测试文件共 77 项通过；前端 6 个文件 30 项 Vitest 通过；权限矩阵及台账服务端拒绝/只读的 Chromium E2E 7 项通过；Hermes 动态工具契约 6 项通过。飞书与导出外部边界均使用 mock，未写真实业务或外部数据。
- 定向 Ruff、5 个后端源文件 Mypy、前端构建与 TypeScript 检查通过。定向 ESLint 零错误，保留详情/列表的 5 项历史警告；没有放宽 lint 门禁。包含整个工作区未提交与未跟踪改动的测试影响检查：缺失测试组及未分类路径均为 0；`git diff --check` 通过。根 API 生成脚本执行成功，三个生成产物与执行前哈希一致。开发库已确认在 `f7b2d9a4c103` 单 head；本轮未新增 migration、环境变量或手工修改生成文件，未重复做迁移升降级和全量覆盖率。首批模块保持草稿。

### 发布目录与运行时策略校验（2026-08-31）

- 先通过新增测试复现目录缺口：原发布检查依赖 OpenAPI 文档，遗漏隐藏接口、无法识别被文档合并的重复路由；跨模块页面、空范围适配器、高风险动作只要求查询等错误绑定也未被统一拒绝。
- 改为读取实际挂载的 HTTP 路由，保留隐藏接口、重复入口及子应用挂载前缀；无法展开的 ASGI 挂载保留为未登记项。不再依赖 OpenAPI 缓存决定目录完整性。
- 发布前识别重复实际路由、已不存在接口的旧策略、无效或跨模块页面、重复页面绑定、空白范围适配器及非法权限。高风险动作必须要求操作权限，修改/删除不能只要求查询，删除必须独立登记删除子权限。没有根据路径名批量补齐业务绑定。
- 运行时使用相同绑定校验，并拒绝实际目录中的重复处理入口；新增真实 AsyncClient 测试确认隐藏未登记、重复、错误高风险/写入权限、跨模块绑定均在业务处理前返回 403。
- 发布摘要加入排序后的真实接口目录，并读取当前页面接口策略；接口数量和缺口数量相同但目标接口变化时摘要仍变化。策略变化后的旧发布预览通过真实发布接口返回 409，未发生数据库写入。
- 8 个定向测试文件覆盖目录、接口强制、权限计算、版本化授权写入、HR 员工、仓储物料及采购范围：80 项通过。定向 Ruff、4 个触达源码文件的 Mypy（`--follow-imports=silent`）和 `git diff --check` 通过。
- 本轮修改范围的测试影响检查通过；全工作区检查当前仍有设备、生产、注册、飞书集成四组缺少对应测试变更，属于本轮以外的工作区改动，未增加无关测试或调整门禁规则来消除。
- 开发库已确认在当前 Alembic head。本轮没有改变接口请求/响应 Schema、新增 migration 或环境变量，未重新生成 API；未重复执行全量构建、迁移升降级或 E2E。所有首批模块仍须完成实际菜单、业务范围和工具接入后再发布。

### 用户与角色权限抽屉异步隔离（2026-08-31）

- 回归测试先复现：切换角色后旧确认框仍能提交；已提交的旧角色请求迟到时会覆盖当前角色编辑结果。用户抽屉存在同类保存响应风险。
- 两个抽屉均按当前加载会话核验确认框、保存结果和错误结果。切换对象、关闭或刷新后，旧回调失效；返回对象 ID 不匹配时明确报错，不展示其他对象的授权。
- 增加保存请求锁，重复确认只提交一次；保存期间禁用权限、部门、高风险动作、批量按钮和调整原因，防止提交后的响应覆盖刚输入的修改。已经发往后端的合法请求不会因切换对象而取消，本轮只隔离其前端结果，仍由后端版本与权限校验保障写入。
- 409 保留本地修改与原因，不自动重试覆盖；父组件仅传入同一用户/角色的新对象实例不再触发重新加载，避免无意丢失草稿。角色初始加载、失败与重载状态明确区分。
- 实际使用 Ant Design 组件的交互测试覆盖迟到读取、旧确认、迟到保存成功/失败、重复提交、同对象重渲染、错配返回、正常保存和版本冲突。连同既有矩阵、中文模块名称、差异预览及管理界面测试，共 7 文件、32 项通过。
- 两个组件及两个新增交互测试文件定向 ESLint 零错误、零警告；`tsc --noEmit`、`git diff --check`、工作区测试影响检查通过。未改路由或 Server/Client 边界，未重复运行构建与全量 E2E；没有新增 migration、OpenAPI、生成类型或环境变量变化。
- HR 员工档案已绑定实际叶子路由 `/hr/profile` 和页面键 `hr:employee-management:profile`；旧目录身份已退役，飞书联系人使用独立叶子。首批业务接口/工具完整接入与实际数据库菜单核对仍为发布前置条件。

### HR 员工档案增量完善（2026-08-31）

- 为 `hr:employee-management:profile` 登记 8 个精确接口绑定；已发布页面不再被旧 HR 管理通配或培训部门别名扩大权限，草稿仍保留原行为。其他 HR 接口未登记即拒绝，不能据此发布整个 HR 模块。
- 列表、统计、工号查询、详情、创建、修改、删除和单条同步使用相同部门 ID 解析结果，包含授权部门的下级。记录以非空子部门优先、否则主部门作为归属，防止主部门命中后放行其他子部门。显式查询无权部门及读写范围外对象返回 403。
- 修改、删除和单条同步在原记录行锁内重新读取最新归属；修改后的部门也必须在范围内。普通编辑不能变更员工状态或合同意见，必须使用相应业务流程；表单提交未变化的值不被误拦。
- 创建、修改、删除、同步写入持久业务审计，记录操作人、对象、页面及变更字段名，不保存员工敏感字段正文。员工高风险动作改为“删除员工档案”“同步员工档案至飞书”等具体中文名称。
- 测试在独立 `dazah_test` 执行真实 AsyncClient 路由和 SQL，飞书全部 fake。覆盖范围内 CRUD、下级部门、跨部门拒绝、普通操作不能越过删除/同步子权限、业务状态旁路拒绝、审计和未登记接口拒绝。
- 执行员工页面权限、旧 HR 页面访问兼容、HR Service/Repository 触达路径、页面权限计算、接口强制和页面目录共 9 个测试文件：76 项通过。接口强制测试显式模拟空目录后验证未登记拒绝，不再依赖员工接口尚未登记的旧假设。
- 定向 Ruff 通过；5 个触达源码文件以 `mypy --follow-imports=silent` 检查通过。默认跟随依赖的 Mypy 被现有 `recruitment_service.py:484` 的 PyMuPDF 导入类型注释错误阻断，未修改无关招聘代码。
- 本轮没有新增 migration、环境变量或改变请求/响应 Schema，因此未重复生成 API、构建前端或执行完整迁移流程；开发库已确认在工作区当前 Alembic head。工作区测试影响检查缺失测试组和未分类路径均为 0。
- 新增真实数据库用例还发现既有结构漂移：ORM/请求允许空工号，但历史 migration 创建的 `hr.employees.employee_number` 为 NOT NULL；空工号创建会触发完整性错误。当前成功路径使用非空工号，未通过手工改库掩盖此问题；需要单独审查增量 migration 和空工号业务规则后才能算员工档案完整验收。
- 尚未接入 HR 培训、招聘、合同流程、批量导入导出、其余统计和 Agent 工具；历史员工目录与新叶子页面的不一致仍需核对。外部同步与数据库不构成分布式原子事务，此轮不宣称四模块已完成发布验收。

### 仓储增量完善（前轮）

- 明确登记 33 个仓储物料菜单页到已有数据别名的映射；请求路径中的数据页必须与可信授权页面一致，不能借用已授权页面读取其他数据页。
- 16 个现有车间/部门物料页使用“车间”字段校验部门；其余已核对的物料页明确范围“不适用”，不提供虚假部门配置。分析、仪表盘、动态数据绑定等尚未完整适配，仓储发布门禁继续关闭。
- 本地快照、飞书实时结果、缓存命中、飞书失败回退均在分页、总数和统计前应用同一部门范围；缓存不会被某个用户的过滤结果覆盖。显式指定未授权车间的等值查询返回 403。
- 详情校验记录部门；受限用户的修改/删除先校验原始记录，禁止更改归属车间，但允许表单提交未改变的车间值。跨页面、跨部门请求在外部写入前拒绝。
- 强制刷新单独要求“操作 + 同步”授权。前端普通刷新不触发飞书强制同步；同步按钮二次确认，删除按钮独立授权，访问但不可查询不发起客户端取数。
- 静态检查：定向 Ruff、4 文件 Mypy、前端 TypeScript 通过；组件 ESLint 零错误，9 项历史警告与 HEAD 基线一致，没有新增警告。
- 后端使用独立 `dazah_test` 执行 `tests/modules/warehouse`、`test_page_permissions.py`、`test_page_permission_enforcement.py`、`test_page_permission_catalog.py`：259 项通过。新增 AsyncClient 用例验证 401/400/403、正常详情与写入、跨页/跨部门拒绝、删除独立权限、同步独立权限及等值筛选越权；飞书调用均为 fake，未操作真实外部业务记录。
- 前端 `WarehouseFeishuTablePage.test.tsx`、`actions/warehouse.test.ts`、`stores/auth.test.ts` 合计 13 项通过；验证只访问、只读、普通操作、删除/同步子权限和确认前不调用同步。
- 根 API 生成脚本完整执行成功；开发库已确认在当前 Alembic head，本轮未增加数据库结构或环境变量。没有重新跑完整构建/E2E：本轮不改变路由或 Server/Client 边界，采用组件和真实 AsyncClient 路由的定向证据。
- 外部飞书记录的读取检查与后续写入不构成分布式原子事务；飞书侧并发变更归属、动态数据表绑定变更、高风险持久业务审计仍需后续验收，不作为仓储整体可发布的证据。

### 前序实施验证

- 定向 Ruff：通过；新权限计算、目录、Repository、请求依赖、Agent 工具/范围服务的 Mypy：通过。
- AsyncClient 回归覆盖未登录、缺少页面上下文、未登记接口、同模块伪造页面、重复接口绑定、本人角色间接提权。
- 开发容器使用独立 `dazah_test` 执行身份与采购测试集、页面 Agent 执行/目录、工具执行器、Agent 基础契约、模块访问集成和审计分类：306 项通过，4 项原有条件跳过。使用仓库原 Pytest 配置；版本化写接口通过 savepoint 隔离，真实验证角色基线、用户拒绝/恢复、409、401/403、审计与 outbox。
- 前端 23 个定向 Vitest 文件、126 项测试通过；定向 ESLint 与 TypeScript 检查通过。
- Playwright Chromium `e2e/identity/page-permissions.spec.ts`：开发模式和本地 standalone 生产构建模式均为 4 项通过，覆盖全部模块中文名称、差异确认、409 保留编辑、无访问/无查询在取数前拒绝、只读入口与同模块越权 URL。外部服务使用本地 mock，未连接真实飞书/LLM。临时测试服务已停止。
- Next.js 构建成功，验证代理和 Server Component 路由边界。
- Hermes `tests/test_dazah_backend_contract.py`：6 项通过，继续动态使用后端 search/describe/execute 契约，没有新增业务白名单或修改 Hermes 工具注册。
- Alembic 单 head 已确认；本任务 migration 的升级/降级/再升级在 8 月 28 日验证，本次将独立测试库更新到工作区当前 head。没有再次降级其他任务新增的 migration。
- 根 `scripts/generate-api.ps1` 修正后完整成功执行，后端 OpenAPI 和前端生成类型保持同步；没有手工改生成类型。
- `check-test-impact.py --base HEAD --head HEAD` 只验证提交区间，不能作为未提交修改证据；另外使用脚本相同 `evaluate()` 对当前 diff 与未跟踪文件检查，缺失测试组和未分类路径均为 0。
- `git diff --check` 通过。没有执行 Git 提交、推送或切换分支。

尚未执行首批所有页面跨部门修改验收、完整关键 E2E 和全量覆盖率；当前证据只支持上述基础设施与定向行为，不支持“首批四模块已全部接入”的结论。

本任务涉及 migration、OpenAPI 和前端生成类型；没有新增环境变量。工作区其他部署、身份服务、Hermes 和 EDBO 退役变更保持原样。
